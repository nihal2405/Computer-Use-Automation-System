# Mock banking application scaffold

Implementation is pending. Build a local target with synthetic records and a
search → detail → accounts → balance workflow. The automation package must use
its visible UI; it must never import this application's fixtures to obtain data.

Templates and styles will be added during implementation. Include reproducible
not-found, delayed-load, permission-denied, session-expired, and unexpected-dialog
scenarios. The dialog scenario must support real same-session human intervention.
